{
  "name": "quran-app-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security rules tests for firestore.rules; run with the Firestore emulator.",
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --only firestore --project demo-quran-rules \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0"
  }
}
